{
  "name": "ai-phone",
  "version": "2.2.0",
  "description": "Voice interface for Local AI via SIP/FreePBX",
  "private": true,
  "scripts": {
    "test": "npm run test:cli && npm run test:voice-app",
    "test:cli": "cd cli && npm test",
    "test:voice-app": "cd voice-app && node --test test/**/*.test.js",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "lint:voice-app": "eslint voice-app/",
    "precommit": "npm run lint",
    "prepare": "husky"
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "husky": "^9.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "author": "MadLab",
  "license": "MIT"
}